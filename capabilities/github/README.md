# GitHub (Brave)

- **Browser:** Brave (logged-in session in your profile).
- **Start URL:** see `ROUTER.json`.
- **Next:** implement `run.py` (Playwright with Brave user-data dir, or `browser_control`).
